#!/bin/sh
# Wrapper generated by install.sh; the path below points at the installed package.
PYTHONPATH="@LIBDIR@${PYTHONPATH:+:$PYTHONPATH}"
export PYTHONPATH
exec python3 -m notification_history.logger "$@"
